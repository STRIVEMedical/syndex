// Homing sequence for the X/Y/Z axes: driver-level/custom two-stage
// (fast approach + reduced-speed re-approach) homing, per-axis zero
// offset, and homing error/timeout handling.
// (driver-level vs. custom homing not determined yet)
//
// For: Base ESP32

#ifndef HOMING_H
#define HOMING_H

#endif